<!--<%= simple_form_for [topic, post] do |f| %>
<% if post.errors.any? %>
		<div class="alert alert-danger">
			<h4>There are <%= pluralize(post.errors.count, "error") %>.</h4>
			<ul>
				<% post.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
	<%= f.input :title, error: false, input_html: { class: 'form-control' } %>
	<%= f.input :body, error: false, input_html: { class: 'form-control' } %>
	<%= f.button :submit, "Save", class: "btn btn-success" %>
<% end %>-->

<%= form_for [topic, post] do |f| %>
	<% if post.errors.any? %>
		<div class="alert alert-danger">
			<h4>There are <%= pluralize(post.errors.count, "error") %>.</h4>
			<ul>
				<% post.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
	<div class="<%=errors_class(post.errors[:title])%>">
		<%= f.label :title %>
		<%= f.text_field :title, class: 'form-control', placeholder: "Enter post title" %>
	</div>
	<div class="<%= errors_class(post.errors[:body]) %>"
		<%= f.label :body %>
		<%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter post body" %>
	</div>
	<!--<div class="<%= errors_class(post.errors[:image]) %>"
		<%= f.label :image %>
		<%= f.file_field :image %>
	</div>-->
	<div class="form-group">
		<%= f.submit "save", class: 'btn btn-success' %>
	</div>
<% end %>